function (context, args) {
    // example output
    let output = "`NLOCK_UNLOCKED` c001\n`NLOCK_UNLOCKED` EZ_21\n`VLOCK_ERROR`\n`V\"pzqsa0\"` is not the correct security k3y.";

    let lock_unlocked = "`NLOCK_UNLOCKED`",
        lock_error = "`VLOCK_ERROR`";

    // how parse that mofo???
    

    // how parse that mofo???
    let mofo = output;
    return parseThatMofo(mofo);
}
